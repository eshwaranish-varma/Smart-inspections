# ── PostgreSQL (user fills these in) ──
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_inspections
DB_USER=
DB_PASSWORD=

# ── JWT ──
JWT_SECRET=smart_inspections_jwt_secret_change_in_production_2026
JWT_EXPIRES_IN=7d

# ── Email (SMTP) ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sathyapalpeddakkagari@gmail.com
SMTP_PASS="myhm uyfv ozfj wwjd"
EMAIL_FROM=noreply@smart-inspections.fda.gov

# ── App ──
NEXT_PUBLIC_APP_URL=http://localhost:3000
