{
  "version": 2,
  "builds": [
    {
      "src": "backend/app/main.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install -r backend/requirements-prod.txt"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "backend/app/main.py" }]
}
