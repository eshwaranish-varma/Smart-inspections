fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic>=2.6.0
pydantic-settings>=2.2.0

# LLM / AI
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0
langchain-community>=0.0.20
openai>=1.12.0
google-generativeai>=0.5.0
tiktoken>=0.6.0
faiss-cpu>=1.8.0

# PDF / OCR
pymupdf>=1.23.0
pytesseract>=0.3.10
Pillow>=10.0.0
# opencv-python-headless — optional, needed only for handwritten OCR preprocessing

# Document generation
python-docx>=1.1.0
openpyxl>=3.1.0

# CORS
starlette>=0.36.0

# Evaluation (WER for optional ground-truth scoring)
jiwer>=3.0.0

# Fuzzy matching (evidence post-process)
rapidfuzz>=3.6.0

# Local embeddings — optional: pulls in torch (~800MB).
# Uncomment if running on >=2GB RAM instances. Falls back to OpenAI embeddings when absent.
# sentence-transformers>=2.6.0
numpy>=1.26.0

# Utilities
aiofiles>=23.2.0
python-multipart>=0.0.9
httpx>=0.27.0
requests>=2.31.0
supabase>=2.0.0
gunicorn>=21.2.0
